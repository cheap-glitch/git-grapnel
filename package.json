{
  "private": true,
  "name": "git-grapnel",
  "description": "A collection of useful git hooks written in Bash.",
  "version": "1.0.0",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cheap-glitch/git-grapnel.git"
  },
  "bugs": {
    "url": "https://github.com/cheap-glitch/git-grapnel/issues"
  },
  "author": "cheap glitch <cheap.glitch@gmail.com> (https://github.com/cheap-glitch)",
  "homepage": "https://github.com/cheap-glitch/git-grapnel#readme",
  "engines": {
    "bash": "^5.1.16"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "coverage": "bashcov test/test.bash",
    "lint": "find src test '(' -type f -and -executable -or -name '*.bash' -or -name '*.bats' ')' -print0 | xargs --null -- shellcheck --enable=all --external-sources",
    "test": "bats test",
    "test:watch": "watchexec --watch src --watch test --clear -- npm test"
  },
  "devDependencies": {
    "bats-assert": "^2.0.0"
  }
}
